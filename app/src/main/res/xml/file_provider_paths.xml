<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 应用内部缓存目录：用于安装更新 APK -->
    <cache-path name="cache_apks" path="." />
    <!-- 应用外部缓存目录（兼容备选路径） -->
    <external-cache-path name="external_cache_apks" path="." />
    <!-- 应用专属 files 目录（兼容） -->
    <files-path name="files_apks" path="." />
</paths>
